// @firstmom/shared
// Cross-package types and constants. Real content (prompts, safety, types) lands in M2/M3.

export const PACKAGE_NAME = '@firstmom/shared';

export type Locale = 'ko' | 'en';

export type ChildStage = 'infant' | 'baby' | 'toddler' | 'preschool';
